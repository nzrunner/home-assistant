name: "TOC Generator"

on:
  workflow_dispatch:
  push:
    paths:
      - "docs/*.md"
      - "README.md"

jobs:
  generateTOC:
    name: 🗂 Generate TOC
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4.3.1
        with:
          TOC_TITLE: 'Table of Contents'
          FOLDING: true
